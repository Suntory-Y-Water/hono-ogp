/**
 * OGP related types and utilities (Client-safe)
 */

// Re-export constants and types from constants file
export { GRADIENT_PRESETS, type GradientPresetName } from '@/lib/constants';

// Re-export server types for compatibility
export type { OGPImageOptions } from '@/lib/ogp-server';
